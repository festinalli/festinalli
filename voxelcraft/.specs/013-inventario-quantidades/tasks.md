# Tasks 013 — Drops + inventário com quantidades

- [x] T1 — `src/player/inventory.js` puro (make/count/add/take/JSON).
- [x] T2 — `editing.js`: drop ao quebrar, consumo ao colocar (gate em 0).
- [x] T3 — `main.js`: cria inventário (save/default), renderHotbar com quantidade.
- [x] T4 — `persistence.js`: inclui `inv` no save (retrocompatível).
- [x] T5 — index.html: estilo do badge de quantidade / slot zerado.
- [x] T6 — ADR 0009 (inventário finito).
- [x] T7 — `test/inventory.test.js` + ajuste persistence.test (inv).
- [x] T8 — `npm test` + `npm run build` verdes.
- [ ] T9 — Validação manual (quebrar soma, colocar consome) — humano.
- [x] T10 — Atualizar LOOP (013 → feito).
